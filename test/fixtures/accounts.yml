DEFAULTS: &DEFAULTS
  email: $LABEL@example.com
  password_digest: <%= BCrypt::Password.create(APP::DEFAULT_PASSWORD) %>

  email_confirmation_at: now
  email_confirmation_token: <%= 'Account.new.email_confirmation_token' %>-$LABEL

mary:
  <<: *DEFAULTS

john:
  <<: *DEFAULTS
