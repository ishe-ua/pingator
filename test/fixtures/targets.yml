DEFAULTS: &DEFAULTS
  user: mary
  url:       http://example.com/$LABEL
  checktime: <%= Target.new.checktime %>

  verified_at: now
  verification_token: <%= Target.new.verification_token %>-$LABEL

mary:
  <<: *DEFAULTS
  user: mary

john:
  <<: *DEFAULTS
  user: john
