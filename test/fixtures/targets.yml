DEFAULTS: &DEFAULTS
  user: mary
  url:  http://example.com/$LABEL
  plan: <%= Target.new.plan %>

  verified_at: now
  verification_token: <%= Target.new.verification_token %>-$LABEL

mary:
  <<: *DEFAULTS
  user: mary

john:
  <<: *DEFAULTS
  user: john
