<%= render 'shared/title', text: 'My Urls' %>

<% if current_user.targets.any? %>
  <%= link_to 'Add new', new_target_path, class: "add" %>

  <ul>
    <% current_user.targets.order(created_at: :desc).each do |target| %>
      <li class="target"><%= render target %></li>
    <% end %>
  </ul>

<% else %>

  <%= render 'shared/any', text: 'Add first and start monitoring' %>

  <%= link_to new_target_path do %>
    <button>Add</button>
  <% end %>

<% end %>
