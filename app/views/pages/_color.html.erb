<%= link_to url, class: "color #{name}" do %>
  <%= image_tag "colors/#{name}.svg", width: "210px" %>

  <% if block_given? %>
    <div class="text">
      <%= yield %>
    </div>
  <% end %>
<% end %>
